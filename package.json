{
  "name": "habit-sprint-app",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node dist/smoke-test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
